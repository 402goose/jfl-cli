{"v":1,"ts":"2026-03-11T00:42:00.000Z","session":"hathbanger/pr-sentinel","type":"fix","status":"complete","title":"PR Sentinel → JFL integration: write review:findings to jsonl","summary":"Updated pr-sentinel.yml to write review:findings events to .jfl/service-events.jsonl after review runs. Removed dead hub POST from jfl-eval.yml. Hub doesn't run in CI — CI writes to jsonl files, commits, hub ingests locally via file watcher on git pull.","detail":"Three changes: (1) pr-sentinel.yml gets id on action step, captures review_json/findings_count/action outputs, writes structured review:findings event to .jfl/service-events.jsonl with severity counts and finding summaries, commits + pushes. Uses env vars (not shell interpolation) for safe JSON handling. (2) jfl-eval.yml drops the 'Post eval:scored event to hub' step that tried to curl the hub API — hub doesn't run in CI. Also removed unused JFL_HUB_URL/TOKEN secrets and env vars. (3) Auto-merge gate updated to check both github-actions and github-actions[bot] author logins for PR Sentinel review state.","files":[".github/workflows/pr-sentinel.yml",".github/workflows/jfl-eval.yml"],"learned":["Hub doesn't run in CI. CI writes to .jfl/*.jsonl files and commits. Hub ingests locally via file watcher when someone pulls.","PR Sentinel outputs review_json, findings_count, action — pass via env vars to avoid shell injection from JSON content"]}
